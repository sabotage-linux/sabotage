[build]
dest="$butch_install_dir""$butch_prefix"
mkdir -p "$dest"/bin
for i in $(ls "$K"/bin |grep -v butch); do
	install -Dm 755 "$K"/bin/"$i" "$dest"/bin/$(basename "$i")
done
cp -rf "$K"/share "$dest"
